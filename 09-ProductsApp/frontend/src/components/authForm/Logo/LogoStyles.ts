import {StyleSheet} from 'react-native';

export const useStyles = () =>
  StyleSheet.create({
    image: {
      resizeMode: 'contain',
      width: 140,
      height: 140,
      marginBottom: 5
    },
  });
