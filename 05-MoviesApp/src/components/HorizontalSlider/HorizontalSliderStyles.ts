import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 30
  },
  title: {
    fontSize: 22,
    marginLeft: 20,
    marginBottom: 5,
    fontWeight: 'bold'
  }
})
